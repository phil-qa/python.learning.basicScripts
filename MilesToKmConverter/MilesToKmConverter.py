#Convert the value in miles to Kilometers using the multiplier 1.6 * the value

distance = input ('How many miles :')

distance = int(distance)

km = 1.6* distance

print(f'The distance for {distance} as Km is {km}')